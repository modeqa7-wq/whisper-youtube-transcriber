services:
  - type: web
    name: whisper-youtube-transcriber
    env: python
    buildCommand: |
      apt-get update && apt-get install -y ffmpeg
      pip install -r requirements.txt
    startCommand: "uvicorn app:app --host 0.0.0.0 --port 10000"
